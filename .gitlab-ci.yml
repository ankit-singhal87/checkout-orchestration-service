stages:
  - validate

default:
  image: alpine:3.20
  before_script:
    - apk add --no-cache git

validate:scaffold:
  stage: validate
  script:
    - sh scripts/ci/validate-scaffold.sh
  rules:
    - when: always
